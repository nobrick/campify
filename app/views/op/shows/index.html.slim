- content_for :panel_heading
  | 活动列表
  .pull-right
    = link_to '新建活动', new_op_show_path, class: 'btn-sm btn-primary'
- content_for :panel_body
  table.table
    thead
      tr
        th 活动ID
        th 名称
        th 描述
        th 发起人
        th 类别
        th
        th

    tbody
      - @shows.each do |show|
        tr
          td = show.id
          td = link_to show.name, [ :op, show ]
          td = show.description
          td = show.proposer.username
          td = show.category
          td = link_to '编辑', edit_op_show_path(show)
          td = link_to '删除', [ :op, show ], data: { confirm: '你确实要删除这个活动吗？' }, method: :delete
